name: swift_flutter
description: A reactive state management library for Flutter with automatic dependency tracking, computed values, async state, form validation, and more.
version: 1.1.1
homepage: https://github.com/ravikinha/swift_flutter
repository: https://github.com/ravikinha/swift_flutter
issue_tracker: https://github.com/ravikinha/swift_flutter/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# Package configuration
flutter:
  # This package does not use any assets
  # assets:
  #   - images/
